---
label: article-partners
hide_body: true
fields:
- name: headline
  type: text
  config:
    required: false
  label: headline
- name: content
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: html
  label: content
- name: blue-background
  type: boolean
  label: blue-background
- name: list
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: false
    label: name
  - name: photo
    type: file
    config:
      maxSize: 250
    label: photo
  config:
    min: 
    max: 
    labelField: 
  label: list
